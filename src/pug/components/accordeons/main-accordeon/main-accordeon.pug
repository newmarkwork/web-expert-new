mixin main-accordeon(data)
  .main-accordeon(class= data.class ? data.class : null)
    each item in data.items
      .main-accordeon__item
        .main-accordeon__item-header.collapsed
          span!= item.title
        .main-accordeon__item-body
          .main-accordeon__item-body-content!= item.text
